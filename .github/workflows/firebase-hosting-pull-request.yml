# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on push
"on":
  pull_request:
    branches:
      - master
    types:
      - closed
env:
  VITE_APIKEY: ${{ secrets.REACT_APP_APIKEY }}
  VITE_AUTHDOMAIN: ${{ secrets.REACT_APP_AUTHDOMAIN }}
  VITE_DB: ${{ secrets.REACT_APP_DB }}
  VITE_PID: ${{ secrets.REACT_APP_PID }}
  VITE_SB: ${{ secrets.REACT_APP_SB }}
  VITE_SID: ${{ secrets.REACT_APP_SID }}
  VITE_APPID: ${{ secrets.REACT_APP_APPID }}
  VITE_MID: ${{ secrets.REACT_APP_MID }}
  VITE_BACK: ${{ secrets.REACT_APP_BACK }}
jobs:
  build_and_deploy:
    if: github.event.pull_request.merged
    runs-on: ubuntu-latest
    steps:
      - uses: SpicyPizza/create-envfile@v1
        with:
          envkey_DEBUG: false
          VITE_APIKEY: ${{ secrets.REACT_APP_APIKEY }}
          VITE_AUTHDOMAIN: ${{ secrets.REACT_APP_AUTHDOMAIN }}
          VITE_DB: ${{ secrets.REACT_APP_DB }}
          VITE_PID: ${{ secrets.REACT_APP_PID }}
          VITE_SB: ${{ secrets.REACT_APP_SB }}
          VITE_SID: ${{ secrets.REACT_APP_SID }}
          VITE_APPID: ${{ secrets.REACT_APP_APPID }}
          VITE_MID: ${{ secrets.REACT_APP_MID }}
          VITE_BACK: ${{ secrets.REACT_APP_BACK }}
          file_name: .env
      - uses: actions/checkout@v2
      - run: npm install
      - run: npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_FLUIRZEN }}'
          channelId: live
          projectId: fluirzen
